# Take input as users name
# save the name in variable
# pass the variable through a function and print "Hello _____"

ip = input("Enter ip address ")
def nmap(ip):
    print(f"Attacking {ip}")

nmap(ip)